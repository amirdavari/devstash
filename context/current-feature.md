# Current Feature

_No active feature. Document the next one here._

## Status

None — awaiting next feature.

## Goals

_TBD_

## Notes

_TBD_

## History

<!-- Keep this updated. Latest to earliest (newest first). -->

- **2026-07-12 19:45:00 — Dashboard Items · real data (completed).** Swapped the dashboard main-area item data from mock to live Neon data via Prisma on branch `feature/dashboard-items`, keeping the existing design. Added `src/lib/db/items.ts` with two server-side functions: `getRecentItems(limit = 24)` — most-recently-used items for the current user, ordered by `lastUsedAt` desc (nulls last) then `createdAt` desc, each shaped into `ItemCardData` (id, title, per-item type `{name, icon, color}`, a `detail` meta string derived from language / URL host / file size / type label, first `collectionName`, plus `isFavorite`/`isPinned`); and `getDashboardStats()` returning live counts (`totalItems`, `collections`, `favorites` = favorited items, `recentlyUsed` = items with `lastUsedAt` set). Like `db/collections.ts`, both resolve the seeded **demo user** (`demo@devstash.io`) as an auth stand-in (documented `TODO` to swap for the Auth.js session user), returning empty/zero if missing. Made `stats-cards.tsx` an async server component fetching `getDashboardStats()` (replacing mock `dashboardStats`); reworked `recent-items.tsx` to take DB items as a prop, keep the client **All / Pinned / Favorites** filter, and render nothing when a filter (or the whole list) is empty (per spec — no empty-state message); updated `item-card.tsx` to consume `ItemCardData` with icon/border/label derived from the item's DB type row; and rewired `page.tsx` to fetch `getRecentItems()` alongside the greeting counts and pass it down. Also removed a leftover `.slice(0, 10)` display cap in `recent-items.tsx` (inherited from the mock, which only ever had 9 items so it never triggered) that was hiding 8 of the 18 seeded items — the fetch still bounds the preview at 24. `mock-data.ts` is untouched but its `recentItems`/`dashboardStats` exports are now unused (still feeds the sidebar via `currentUser`/`itemTypes`/`collections`). `npm run build` passes; verified in the browser — `/dashboard` renders all 18 live items with correct host/language meta lines and stat counts (18 items / 5 collections; Favorites/Recently-used are `0` and the Pinned/Favorites filters show nothing because the seed has no favorited/pinned items or `lastUsedAt`). Spec: `@context/features/dashboard-items-spec.md`.
- **2026-07-12 18:30:00 — Dashboard Collections · real data (completed).** Swapped the dashboard collections section from mock data to live Neon data via Prisma on branch `feature/dashboard-collections`, keeping the existing design. Added `src/lib/db/collections.ts` with three server-side data functions: `getRecentCollections(limit = 6)` (most-recently-updated collections for the current user, each shaped into `CollectionCardData` — id, name, `itemCount`, `isFavorite`, a per-type `breakdown` tallied from the collection's items and sorted most-used-first, and an `accentColor` derived from the dominant type's color with a gray `#6b7280` fallback for empty collections), plus `getCollectionCount()` and `getItemCount()` for the greeting summary line. Auth isn't wired yet, so all three resolve the seeded **demo user** (`demo@devstash.io`) as a stand-in (documented `TODO` to swap for the Auth.js session user), returning empty/zero if that user is missing. Rewired `src/app/dashboard/page.tsx` to `async` + `export const dynamic = "force-dynamic"`, fetching `getItemCount()`/`getCollectionCount()` in parallel for the summary line; made `CollectionsSection` an async server component that awaits `getRecentCollections()`; and updated `CollectionCard` to consume `CollectionCardData` (accent-tinted left border + `color-mix` background, favorite star, per-type badges via the shared `typeIcon` helper). Item-type icons/colors now come from the DB row (`ItemType.icon`/`.color`) rather than mock lookups. Items list beneath the cards remains out of scope. `npm run build` passes. Spec: `@context/features/dashboard-collections-spec.md`.
- **2026-07-12 15:25:00 — Seed Data (completed).** Expanded `prisma/seed.ts` on branch `feature/seed-data` to populate the dev database with a demo dataset. Added `bcryptjs` (+ `@types/bcryptjs`). Seeds a **demo user** (`demo@devstash.io` / "Demo User", password `12345678` hashed with bcryptjs 12 rounds, `isPro: false`, `emailVerified: now`, upserted by email); keeps the existing idempotent seed of the **7 system item types**; and creates **5 collections with 18 items** wired through the `ItemCollection` join table — React Patterns (3 TS snippets), AI Workflows (3 prompts), DevOps (1 snippet + 1 command + 2 links), Terminal Commands (4 commands), Design Resources (4 links). Link items use `contentType: URL` + `url` (real URLs); text items use `contentType: TEXT` + `content` with `language` for code. The whole seed is **idempotent** — user keyed by email, collections by `(userId, name)`, items by `(userId, title)`, join rows by composite PK — verified by running it twice with no duplicates (1 user / 5 collections / 18 items = 12 text + 6 links). Also extended `scripts/test-db.ts` (`npm run db:test`) to fetch and print the demo user, a collections summary table, and a per-collection item breakdown. `npm run build` passes; `db:test` confirms the data. (The pre-existing `react-hooks/set-state-in-effect` lint error in `theme-provider.tsx` is unrelated and untouched.)
- **2026-07-12 13:55:00 — Prisma + Neon PostgreSQL Setup (completed).** Set up Prisma 7 ORM against a Neon serverless Postgres database on branch `feature/database-setup`. Installed `@prisma/client@7`, `prisma@7`, the `@prisma/adapter-pg` driver adapter, `pg`, `dotenv`, and `tsx`. Authored `prisma/schema.prisma` with the full initial schema from `@context/project-overview.md` — `ContentType` enum; app models `Item`, `ItemType`, `Collection`, `ItemCollection`, `Tag`, `ItemTag`; and Auth.js models `User`, `Account`, `Session`, `VerificationToken` — with the spec's indexes and cascade deletes. **Prisma 7 breaking changes handled:** new `prisma-client` generator with explicit `output = ../src/generated/prisma` (gitignored, regenerated via `postinstall`); connection URLs moved out of the schema datasource into `prisma.config.ts` (v7 has no `directUrl` there — the config `datasource.url` *is* the CLI/migration URL, pointed at `DIRECT_URL`, while the runtime adapter in `src/lib/prisma.ts` uses pooled `DATABASE_URL`); driver adapter now mandatory; `dotenv` loaded explicitly. Added `src/lib/prisma.ts` (adapter-backed client singleton cached on `globalThis` in dev), `prisma/seed.ts` (idempotent seed of the 7 system `ItemType`s), `scripts/test-db.ts` (connectivity + schema + seed check), and npm scripts `db:generate|migrate|deploy|seed|studio|test`. Created + applied the first migration `20260712120440_init` to the Neon dev branch and seeded the 7 system types. Verified: `npm run build` passes, `prisma migrate status` is in sync, and `npm run db:test` connects and reports 7 system item types. `.env.example` documents all vars with placeholders; real secrets live only in gitignored `.env`.
- **2026-07-12 09:15:30 — Dashboard UI Phase 3 · Main content, theming & typography (completed).** Built the dashboard main content area on branch `feature/dashboard-phase-3`, wired to `src/lib/mock-data.ts`. New page composition (`src/app/dashboard/page.tsx`): time-of-day greeting ("Good morning, Jordan 👋") + summary line, then `StatsCards`, `CollectionsSection`, and `RecentItems`. Components added under `src/components/dashboard/`: `stats-cards.tsx` (4 metrics — total items, collections, favorites, recently used — each with a colored lucide icon), `collections-section.tsx` + `collection-card.tsx` (8 accent-tinted collection cards via `color-mix`, favorite stars, type-breakdown badges, "View all" link), and `recent-items.tsx` (client component) + `item-card.tsx` (up to 10 recent items with type-colored left border, type label, pin/favorite icons, mono meta line, and an **All / Pinned / Favorites** filter). Extracted shared type helpers to `src/lib/item-types.ts` (`typeIcon`, `itemTypeById`) and refactored the sidebar to use them. **Theming:** added a `ThemeProvider` + `ThemeToggle` (`src/components/theme/`), a blocking pre-paint theme init script in `layout.tsx` (defaults dark, persists to `localStorage`, `suppressHydrationWarning` on `<html>`/`<body>`), and a topbar toggle in `DashboardHeader`. **Typography (per design spec):** swapped Geist → **Inter** (with system fallback stack) as `--font-sans`; added a full type scale as Tailwind v4 `--text-*` tokens in `globals.css` (page-title, section-title, card-title, body/meta/caption, sidebar item/label, stat, button — with -0.02em on headings and 0.04em on uppercase labels) and applied the roles across all dashboard components + sidebar (uppercase group labels). **Fixes:** resolved a `react-hooks/static-components` error in `item-card.tsx` by rendering the dynamic icon via `createElement` instead of a render-scope component binding; rewrote `src/hooks/use-mobile.ts` with `useSyncExternalStore` to clear a `react-hooks/set-state-in-effect` error. `npm run build` passes; verified in a real browser (Playwright) — dark/light toggle, computed type scale matches the spec exactly, Pinned filter narrows the grid, and the sidebar renders as a desktop rail vs. mobile off-canvas drawer with no page errors.
- **2026-07-12 07:13:12 — Dashboard UI Phase 2 · Sidebar (completed).** Built the collapsible dashboard sidebar on branch `feature/dashboard-phase-2`, wired to `src/lib/mock-data.ts`. New `DashboardSidebar` (`src/components/dashboard/dashboard-sidebar.tsx`) composed from the shadcn `Sidebar` primitive: brand logo + "DevStash" title, a `#8b5cf6` "New item" button, a search input, a **Types** group (all 7 item types with colored lucide icons, count badges, links to `/items/[type]`), a **Collections** group (5 most-recent collections with accent dots, links to `/collections/[id]`, plus a "new collection" action), and a footer user area (avatar, name, plan, settings link). Added `--color-brand: #8b5cf6` theme token in `globals.css`. Set `collapsible="icon"` so the sidebar collapses to an icon-only rail (labels/search/text hidden); on mobile it renders as a drawer via the primitive. Extracted a client `DashboardHeader` (`src/components/dashboard/dashboard-header.tsx`) that surfaces the reopen trigger only when the sidebar is collapsed or on mobile, and rewired `src/app/dashboard/layout.tsx` to `SidebarProvider` + `SidebarInset`. Fixed a base-ui console warning with `nativeButton={false}` on the link-rendered settings button. `npm run build` passes; `/dashboard` verified in the browser (brand color, icon-rail collapse, single collapse control).
- **2026-07-11 15:30:46 — Dashboard UI Phase 1 (completed).** Scaffolded the dashboard foundation on branch `feature/dashboard-phase-1`. Initialized shadcn/ui (style `base-nova`, neutral base color, CSS-variable theming) — added `components.json`, `src/lib/utils.ts`, and theme tokens in `globals.css`; installed foundational components (`sidebar`, `card`, `separator`, `avatar`, `badge`, `input`, `tooltip`, `scroll-area`, `dropdown-menu`, `skeleton`). Enabled dark mode by default (`dark` class on `<html>`), aligned Geist fonts to `--font-sans`/`--font-mono`, and set real metadata in `layout.tsx`. Added the `/dashboard` route: two-column shell (`src/app/dashboard/layout.tsx`) with a placeholder `DashboardSidebar` component and a placeholder `Main` (`src/app/dashboard/page.tsx`). `npm run build` passes; `/dashboard` verified rendering both placeholders in dark mode.
- **2026-07-11 10:52:18 — Initial setup.** Bootstrapped the Next.js starter, added project context docs (`context/`), customized the starter app (`page.tsx`, `globals.css`, removed default `public/*.svg` assets), updated `CLAUDE.md`, and pushed the initial commits to `origin/main` (https://github.com/amirdavari/devstash.git).